name: Guardar Predicción

on:
  workflow_dispatch:
    inputs:
      data:
        description: 'Datos de la predicción (en JSON)'
        required: true

jobs:
  guardar-prediccion:
    runs-on: ubuntu-latest

    steps:
    - name: Configurar credenciales
      env:
        GITHUB_TOKEN: ${{ secrets.LIGUILLA }}

    - name: Crear una predicción
      run: |
        echo "Guardando predicción en el repositorio..."
